export interface notificationListParams {  
    end: number,
    start: number
}

export interface readAllNotificationParams{
    notificationType: string
}

export interface readNotificationParams{
    notificationId:number  
}